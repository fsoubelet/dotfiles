# https://git-scm.com/docs/git-config

[user]
  name = Felix Soubelet
  email = felix.soubelet@protonmail.com
  signingkey = 90715010

[color]
  # turn on default colors for many commands
  ui = true
  filemode = false
  diff = auto
  status = auto
  branch = auto
  pager = true

[core]
  pager = less
  editor = vim
  # lets you define a global .gitignore for all those *.swp, *~, *.o, etc things that you're frequently sticking in .gitignore
  excludes = ~/.gitignore_global
  # tells git to ignore file permission changes
  filemode = false
  # lets you tweak the default pager, see 'man less' for the meaning of these flags
  pager = 'less -FRSX'
  pager = /usr/bin/less
  excludesfile = /Users/felixsoubelet/.gitignore_global
	autocrlf = input

[github]
  user = fsoubelet

#[pull]
#  rebase = rebase 

[push]
  # Refuse to push if upstream branch name is different from local.
#  default = simple

[rerere]
  # git remembers your partial merge choices and replays them next time
  enabled = true
  autoupdate = true

[merge]
  # personnal favorite merge visualization tool
  tool = kdiff3

[color "diff-highlight"]
	oldNormal = red bold
	oldHighlight = red bold 52
	newNormal = green bold
	newHighlight = green bold 22

[color "diff"]
	meta = yellow
	frag = magenta bold
	commit = yellow bold
	old = red bold
	new = green bold
	whitespace = red reverse

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true

[credential]
	helper = store

[cmiit]
	gpgSign = true

[commit]
	gpgSign = true

[tag]
	gpgSign = true

[gpg]
	program = gpg2
[safe]
	directory = /afs/cern.ch/eng/sl/lintrack/omc_repositories/Beta-Beat.src
	directory = /afs/cern.ch/eng/sl/lintrack/omc_repositories/omc3
	directory = /afs/cern.ch/eng/sl/lintrack/omc_repositories/pylhc.github.io
	directory = /afs/cern.ch/eng/sl/lintrack/omc_repositories/tfs
	directory = /afs/cern.ch/eng/sl/lintrack/omc_repositories/PyLHC
	directory = /afs/cern.ch/eng/sl/lintrack/omc_repositories/sdds
	directory = /afs/cern.ch/eng/sl/lintrack/omc_repositories/optics_functions
	directory = /afs/cern.ch/eng/sl/lintrack/omc_repositories/generic_parser
	directory = /afs/cern.ch/eng/sl/lintrack/omc_repositories/submitter
	directory = /afs/cern.ch/eng/sl/lintrack/omc_repositories/MESS
	directory = /afs/cern.ch/eng/sl/lintrack/omc_repositories/Beta-Beat.src
	directory = /afs/cern.ch/eng/sl/lintrack/omc_repositories/turn_by_turn
	directory = /afs/cern.ch/eng/sl/lintrack/omc_repositories/irnl_rdt_correction
	directory = /afs/cern.ch/user/f/fesoubel/dotfiles
	directory = /afs/cern.ch/eng/acc-models/lhc/2022
